name: Validate OpenClaw Solutions
on:
  pull_request:
  push:
    branches: [main]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python3 scripts/build-search-index.py --check
      - run: python3 scripts/privacy-scan.py
      - run: python3 scripts/claw-search.py "proton popup"
